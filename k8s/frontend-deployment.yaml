---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: study-helper-frontend
  namespace: homework-bot
  labels:
    app: study-helper-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: study-helper-frontend
  template:
    metadata:
      labels:
        app: study-helper-frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/beelzetron/chatbot-studio-helper-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: BACKEND_HOST
          value: study-helper-chatbot
        - name: BACKEND_PORT
          value: "8080"
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: study-helper-frontend
  namespace: homework-bot
spec:
  selector:
    app: study-helper-frontend
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: study-helper-frontend
  namespace: homework-bot
spec:
  to:
    kind: Service
    name: study-helper-frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
